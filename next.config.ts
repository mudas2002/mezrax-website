import { NextConfig } from 'next'

const config: NextConfig = {
  images: {
    domains: [
      'via.placeholder.com',  // Add any other domains here as needed
      'google.com',
      'i.redd.it',
    ],
  },
}

export default config
